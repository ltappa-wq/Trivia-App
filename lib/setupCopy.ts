// Host setup copy: glorious greeting taglines (design prototype TAGLINES).

export const HOST_SETUP_TAGLINES = [
  "Warms up faster than your group chat.",
  "Fewer arguments than family game night. (We tried.)",
  "AI writes it. You argue about it.",
  "No two games are ever the same.",
] as const;

/** Tagline rotation interval (ms) — matches design prototype (~2.2s). */
export const TAGLINE_ROTATE_MS = 2200;
